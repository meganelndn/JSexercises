"use strict";

const name = "Albus Percival Wulfric Brian Dumbledore";
const len = name.length;
const letter = name[0];

console.log(`${name} is ${len} characters long.`)
console.log(`The second character of ${name} is ${name[3]}`);
console.log(`The character at index 6 is ${letter[6]}`);